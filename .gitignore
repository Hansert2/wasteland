node_modules/
.env
.env.*
!.env.example
!.env.prod.example
*.log
.DS_Store

# Generated by tools/page-states.mjs for design work. Rebuilt on demand.
page-states/

# The full-size plate masters, ~26 MB of PNG. Only public/img ships, built from these
# by tools/plates.py — the WebP is lossy, so keep these if you ever want another size.
#
# Anchored. Without the leading slash this matches a directory named img at any depth,
# which includes public/img — the one that has to ship.
/img/
