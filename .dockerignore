# The image is source plus production dependencies. Everything below is either
# rebuilt inside the image, irrelevant to running it, or must never leave this machine.
node_modules
.git
.env
docs
test
tools
scripts
*.md
docker-compose.yml
# The full-size plate masters — 25 MB of PNG that only exists to be resized. What ships
# is public/img, which is built from it.
img
